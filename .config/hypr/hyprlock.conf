general {
    no_fade_in = true
    no_fade_out = true
    hide_cursor = false
    grace = 0
    disable_loading_bar = true
}

background {
    monitor =
    path = $HOME/Pictures/wallpapers/interstallar.jpg  # NOTE only png supported for now
    #color = $color7

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_passes = 2 
    contrast = 1
    brightness = 0.5
    vibrancy = 0.2
    vibrancy_darkness = 0.2
}
# foreground
#image {
   # monitor =
    #path = $HOME/Pictures/wallpapers/nier-automata-ls2.png
    #size = 270
    #border_size = 0
    #rounding = 0
    #rotate = 0
    #reload_time = 0
    #reload_cmd =
    #position = 0, 0
    #halign = center
    #valign = center
#}

# Input Field
input-field {
    monitor =
    size = 220, 40
    outline_thickness = 3
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.35 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgb(89bda6)
    inner_color = rgb(223439)
    font_color = rgb(89bda6)
    fade_on_empty = true
    rounding = -1
    check_color = rgb(204, 136, 34)
    placeholder_text = <i><span foreground="##cdd6f4">Input Password...</span></i>
    hide_input = false
    position = 0, -170
    halign = center
    valign = center
}


# Time
label {
  monitor =
  text = cmd[update:1000] echo "$(date +"%-I:%M")"
  color = rgba(64A66Cee) rgba(d2c5a3ee) 45deg
  font_size = 60
  font_family = FiraCode Nerd Font 
  position = -20, 200
  halign = center
  valign = center
}

# User
label {
    monitor =
    text =  $USER
    color = rgba(64A66Cee) rgba(d2c5a3ee) 45deg
    font_size = 15
    font_family = FiraCode Nerd Font
    position = 0, -210
    halign = center
    valign = center
}
